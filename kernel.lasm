MOV all, ENTRY_MSG
MOV bll, 0xf000
CALL print_string

loop:
	CMP dlll, 0xd
	JMP NOT_EQUAL, loop
HALT

print_string:
	CMP [all], 0
	JMP EQUAL, print_string_end
	MOV [bll], [all]
	ADD bll, 0x1
	ADD all, 0x1
	JMP TRUE, print_string
print_string_end:
	RET

ENTRY_MSG: ws "Executing kernel\0"
