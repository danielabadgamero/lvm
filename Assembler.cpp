#include <vector>
#include <string>

#include "Assembler.h"
#include "Lightning.h"

std::vector<char> Assembler::assemble(std::vector<std::string>& input)
{
	std::vector<char> bin{};

	for (const std::string& line : input)
	{
		line;
	}

	return bin;
}